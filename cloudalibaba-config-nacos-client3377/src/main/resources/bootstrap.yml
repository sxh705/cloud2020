# 高优先级
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: wsl:8848
      config:
        server-addr: wsl:8848
        file-extension: yml
        group: DEV_GROUP
        namespace: 9732d176-2033-4db0-84ca-944225be978b #dev
    refresh:
      enabled: true # 服务会自动重启以更新自身配置

config:
  info: "local"
server:
  port: 3377